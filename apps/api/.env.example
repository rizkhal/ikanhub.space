# IkanHub API — Environment Configuration
# No database required. Runs entirely on JSON metadata + local image files.

# Server
NODE_ENV=development
PORT=3001

# Storage
STORAGE_DIR=../../storage
METADATA_PATH=../../storage/metadata.json

# Public API URL (used for generating absolute URLs)
PUBLIC_API_URL=http://localhost:3001
